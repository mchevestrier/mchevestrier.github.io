export const metadata = {
    title: 'Data',
    order: 3,
    bgColor: 'bg-red-500',
    icon: '<svg fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 8v8m-4-5v5m-4-2v2m-2 4h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path></svg>',
}

import Link from 'next/link';
import Head from 'next/head';

<Head>
  <title>{ metadata.title + ' | Martin Chevestrier' }</title>
</Head>

import { getSortedPostsData } from '../../lib/pages';
export async function getStaticProps() {
  const allPostsData = await getSortedPostsData();
  return {
    props: {
      allPostsData,
    }
  }
}


import numpy from '../../img/data/numpy.png?webp';
import pandas from '../../img/data/pandas.svg';
import scikitlearn from '../../img/data/scikit-learn.png?webp';
import tensorflow from '../../img/data/tensorflow.svg';

import spacy from '../../img/data/spacy.png?webp';
import huggingface from '../../img/data/huggingface.svg';

import pytorch from '../../img/data/pytorch.png?webp';
import r from '../../img/data/r.png?webp';
import julia from '../../img/data/julia.svg';
import airflow from '../../img/data/airflow.png?webp';
import hive from '../../img/data/hive.png?webp';

# Data
<Main>
    <Section>
        ## Machine Learning
        <BlockList>
            <Tile name="NumPy" icon={ numpy } level={ 2 } />
            <Tile name="pandas" icon={ pandas } level={ 2 } />
            <Tile name="scikit-learn" icon={ scikitlearn } level={ 2 } />
            <Tile name="TensorFlow" icon={ tensorflow } level={ 1 } />
        </BlockList>
    </Section>
    <Section>
        ## Natural Language Processing
        <BlockList>
            <Tile name="spaCy" icon={ spacy } level={ 2 } />
            <Tile name="Hugging Face Transformers" icon={ huggingface } level={ 1 } />
        </BlockList>
    </Section>
    <Section>
        ## What I want to learn next:
        <BlockList>
            <Tile name="PyTorch" icon={ pytorch } />
            <Tile name="R" icon={ r } />
            <Tile name="Julia" icon={ julia } />
            <Tile name="Apache Airflow" icon={ airflow } />
            <Tile name="Apache Hive" icon={ hive } />
            {/* <Tile name="NLTK" icon={ nltk } /> */}
        </BlockList>
    </Section>
</Main>